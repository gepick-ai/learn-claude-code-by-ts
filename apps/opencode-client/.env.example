# 开发时 Vite 将 /session、/sse 代理到该地址（与 与-opencode-app-配套对接说明 一致）
OPENCODE_APP_ORIGIN=http://127.0.0.1:3000

# 仅开发：若代理长流式 POST 仍报 `socket hang up`，取消下一行并与上一行同 origin，使浏览器直连 opencode-app
# VITE_OPENCODE_DEV_API_ORIGIN=http://127.0.0.1:3000
