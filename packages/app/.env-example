## Anthropic-compatible
ANTHROPIC_BASE_URL=
ANTHROPIC_API_KEY=

MODEL_ID=

# Optional: sqlite data directory
# If empty, defaults to <packages/app>/.data
APP_DB_PATH=.data

# Optional: project workspace root path
# If empty, defaults to <monorepo>/.projects
APP_PROJECT_PATH=.projects
